# RunPod Serverless handler runtime
runpod
requests

# vLLM is provided by the base Docker image (vllm/vllm-openai:gemma4-cu130).
# For local development outside Docker, install vLLM separately, e.g.:
#   pip install vllm transformers accelerate
